from unittest import TestCase


class TimeTests(TestCase):
    pass

    # def setUp(self):
    # # self.assertTrue(pygame2.init())
    # self.clock = Clock()
    #
    # def test_get_ticks(self):
    #     # ticks = self.clock.get_ticks()
    #     #    self.assertTrue(ticks > 0)
    #     pass
    #
    # def test_wait(self):
    #     ms = time.wait(10)
    #     self.assertTrue(ms > 0)
    #
    # def test_delay(self):
    #     ms = self.delay(10)
    #     self.assertTrue(ms > 0)
    #
    # def test_set_timer(self):
    #     pass
    #
    # def test_Clock_tick(self):
    #     ms1 = self.clock.tick(10)
    #     self.assertTrue(ms1 > 0)
    #     ms2 = self.clock.tick()
    #     self.assertTrue(ms2 - ms1 > 0)
    #     ms = self.clock.tick(10)
    #     self.assertTrue(ms > 0)
    #
    # def test_Clock_tick_busy_loop(self):
    #     ms1 = self.clock.tick_busy_loop(10)
    #     self.assertTrue(ms1 > 0)
    #     ms2 = self.clock.tick_busy_loop(10)
    #     self.assertTrue(ms2 - ms1 > 0)
    #     ms = self.clock.tick(100)
    #     self.assertTrue(ms > 0)
    #
    # def test_Clock_get_time(self):
    #     self.clock.tick(100)
    #     self.clock.tick(100)
    #     ms = self.clock.get_time()
    #     self.assertTrue(ms > 0)
    #
    # def test_Clock_get_rawtime(self):
    #     pass
    #
    #     # self.clock.tick(100)
    #
    # #    self.clock.tick(100)
    # #    ms = self.clock.get_rawtime()
    # #    self.assertTrue(ms > 0)
    #
    # def test_Clock_get_fps(self):
    #     for i in range(0, 10):
    #         self.clock.tick(100)
    #         fps = self.clock.get_fps()
    #         self.assertTrue(fps > 0)
